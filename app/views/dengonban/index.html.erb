<table class="form">
  <%= form_tag(controller: "dengonban", action: "index") do %>
    <tr>
      <td>名前：</td>
      <td> <%= text_field_tag("name") %> </td>
    </tr>

    <tr>
      <td>メール：</td>
      <td> <%= text_field_tag("mail") %> </td>
    </tr>

    <tr>
      <td>メッセージ：</td>
      <td> <%= text_field_tag("msg", '', size: "50x3") %></td>
    </tr>

    <tr>
      <td></td>
      <td> <%= submit_tag("Click") %> </td>
    </tr>
  <% end %>
</table>

<table class="dengon">
  <tr>
      <td style="whdth:50%">メッセージ</td>
      <td>名前</td>
      <td>投稿</td>
      <td>投稿日時</td>
  </tr>
  <% @dengon_data.each do |key, obj| %>
    <td class="msg"><%= ogj['msg'] %></td>
    <td class="msg"><%= ogj['name'] %></td>
    <td class="msg"><%= ogj['mail'] %></td>
    <td class="time"><%= Time.at(key.to_i) %></td>
  </tr>
  <% end %>
</table>
